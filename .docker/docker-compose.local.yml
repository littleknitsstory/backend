version: "3.0"

services:
  pg_l:
    # CHECK PG VERSION
    image: postgres:13-alpine
    environment:
      - POSTGRES_NAME=lks_pg_name
      - POSTGRES_USER=lks_pg_user
      - POSTGRES_DB=lks_pg_db
      - POSTGRES_PASSWORD=lks_pg_pass
      - PGDATA=/var/lib/postgresql/data/pgdata
    ports:
      - ${POSTGRES_PORT}:${POSTGRES_PORT}
    env_file:
      - .env
    volumes:
      # CHECK PATH
      - ./pgdata_3:/var/lib/postgresql/data/pgdata


  redis_l:
    image: redis:6.0.6-alpine
    ports:
      - "6379:6379"
    env_file: .env
    command: redis-server --requirepass ${REDIS_PASSWORD}

  elasticsearch_l:
    image: elasticsearch:7.9.3
    ports:
      - "9200"
      - "9300"
    volumes:
      - ./data/elasticsearch:/data/elasticsearch
      - /dev/log:/dev/log

#  redis-commander:
#    image: rediscommander/redis-commander:latest
#    environment:
#      - REDIS_HOSTS=local:redis:6379
#      - HTTP_USER=root
#      - HTTP_PASSWORD=qwerty
#    ports:
#      - 8081:8081
#    depends_on:
#      - redis

#  flower:
#    container_name: phc_lks_${PROFILE}_flower
#    image: mher/flower
#    command: ["--app src.celery",
#              "--broker=redis://${REDIS_USER}:{REDIS_PASSWORD}@${REDIS_HOST}:${REDIS_PORT}/2",
#              "--port=${FLOWER_PORT}",
#              "--basic_auth=${FLOWER_USER}:${FLOWER_PASSWORD}"]
#    depends_on:
#      - redis
#    ports:
#      - "${FLOWER_PORT}:5555"
